{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_response.filePath // .tool_input.file_path // empty'); case \"$file\" in *.ts|*.tsx|*.js|*.jsx) bunx eslint --fix \"$file\" 2>/dev/null || true; bunx prettier --write \"$file\" 2>/dev/null || true;; esac",
            "timeout": 30,
            "statusMessage": "lint & format 実行中..."
          }
        ]
      }
    ]
  }
}
