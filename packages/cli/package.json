{
  "name": "@magi/cli",
  "private": true,
  "type": "module",
  "bin": {
    "magi": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --watch",
    "test": "bun test"
  },
  "dependencies": {
    "@magi/agent": "workspace:*",
    "@magi/runtime": "workspace:*",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
